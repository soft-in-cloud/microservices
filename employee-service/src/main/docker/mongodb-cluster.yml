version: '2'
services:
  employeeservice-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/employeeservice-mongodb-config --bind_ip 0.0.0.0
  employeeservice-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  employeeservice-mongodb-config:
    image: mongo:4.2.7
    container_name: employeeservice-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
